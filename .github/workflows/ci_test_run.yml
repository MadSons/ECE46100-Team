name: Testing run script options

on:
  pull_request:
  workflow_dispatch:
  

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: checkout code
      uses: actions/checkout@v3
      
    - name: install_case
      run: | 
        cd Phase 1/src/
        ./run install
        
    - name: test_case
      run: | 
        cd Phase 1/src/Phase 1/src/
        ./run test
        
    - name: url_case
      run: | 
        cd Phase 1/src/
        ./run Phase\ 1/reference/SampleUrlFile.txt 
